import React from 'react';

const Nav = () => {
	return (
		<nav className='navbar'>
			<h1>Todo List</h1>
			{/* <p>Currently you have {books.length} books to get through...</p> */}
		</nav>
	);
};

export default Nav;
